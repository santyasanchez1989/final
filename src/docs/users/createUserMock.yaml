paths:
  /api/users/mock:
    get:
      summary: "Crear usuarios mock"
      description: "Este endpoint permite generar usuarios mock para pruebas."
      operationId: "createUserMock"
      tags:
        - "Usuarios"
      responses:
        '201':
          description: "Usuarios mock creados con éxito."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserList'
        '500':
          description: "Error interno del servidor."